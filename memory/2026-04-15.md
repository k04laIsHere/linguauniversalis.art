# 2026-04-15 (Revision 5) - Immersion Gallery Fixes

- **Fixed Navigation Logic**: Refined `ViewModeContext.tsx` to use strict anchor detection (splitting by `?`). `Gallery.tsx` now uses `history.replaceState` for filter updates to prevent accidental mode switches.
- **Lightbox Close Button**: Reduced `z-index` to 999 (below Header) and increased top offset to 140px (Desktop) / 110px (Mobile) to ensure it clears the header layer and follows the Events modal pattern.
- **Thick Framing**: Increased thumbnail borders to 25px in the primary gallery.
- **Lightbox Visuals**: Explicitly removed borders from the Lightbox image to maintain the "White Cube" presentation.
- **Code Quality**: Maintained semantic CSS and respected site-wide layering conventions.
