# 2026-04-15

- Optimized `event_04.jpg` (3.8M -> 103K WebP) and moved original to `backups/heavy_assets/`.
- Overhauled Immersion Gallery (Revision 4):
    - Fixed "All Artists" logic: Refined hash-change listener to only trigger Archive mode on exact matches (#gallery/#archive), preventing gallery filters from hijacking the view.
    - Fixed Lightbox Close Button: Moved to `absolute` inside the `fixed` overlay with explicit `z-index`, ensuring it appears on top of the site header.
    - Fixed Scroll: Enforced `height: 100vh` on `body` during Lightbox open to prevent all residual background movement.
    - Art Presentation: Removed the 40px white border in Lightbox as requested; artworks now occupy the "White Cube" cleanly.
    - Maintained synchronized artist order and persistent dividers across all filter states.
