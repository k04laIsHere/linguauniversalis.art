## 2026-04-04: Lingua Universalis Deployment & Animation Refinement

- **Project:** Lingua Universalis website ([linguauniversalis.art](https://linguauniversalis.art/)).
- **Deployment Fix:** Identified that GitHub Actions were failing to update the `gh-pages` branch for 3 weeks. Resolved by running local production builds and performing forced manual pushes to the `gh-pages` branch.
- **Animation Refactor:** Significantly improved the 'image flip' animation in Gallery Mode. Switched from discrete `onUpdate` triggers to a unified `gsap.timeline` with `scrub: 1.5`. Implemented substantial transition overlap (0.9 units) and changed image container backgrounds to `transparent` to achieve buttery-smooth cross-fades without 'sudden' jumps.
- **Technical Config:** Updated `vite.config.ts` to use port `5174` and added Tailscale (`.ts.net`) and VirtualBox hosts to `allowedHosts` for easier remote testing.
- **Remote Sync:** Synchronized local `master` branch with `origin/main` and ensured remote URLs are kept clean of hardcoded tokens where possible.
